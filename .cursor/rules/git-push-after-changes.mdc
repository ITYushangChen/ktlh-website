---
description: After completing code or content changes in this repo, commit and push to origin.
alwaysApply: true
---

# Git：改完即推送

在本仓库（ktlh-website）完成一批有意义的改动后：

1. 在 `ktlh-website` 目录下执行 `git status` 确认变更。
2. `git add` 需要纳入提交的文件（勿提交 `.env`；`build/`、`node_modules/` 已在 `.gitignore`）。
3. 用清晰的中文或英文写一条提交说明并 `git commit`。
4. 若 `git push` 被拒绝，先 `git pull --rebase origin main` 再 `git push origin main`。
5. 推送成功后再向用户汇报完成。

除非用户明确只要本地修改、不要推送，否则默认应推送到远程。
